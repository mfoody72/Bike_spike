<%= simple_form_for @bike do |f| %>
  <%= f.collection_select :category_id, Category.all, :id, :name, {promt: "Choose a category" }, input_html: { class: "dropdown-toggle" } %>
  <br/>
  
  
	<br/>
	<%= f.file_field :image %>
	<br/>
  <%= f.input :title, label: "The Bike", input_html: { class: "form-control" } %>
  <%= f.input :description, label: "Descripton:", input_html: { class: "form-control" } %>
  <%= f.input :price, label: "Price per hour:", input_html: { class: "form-control" } %>
   <%= f.button :submit %>
  <% end %>
<%= simple_form_for @bike, :url => bikes_path, :html => { :multipart => true } do |f| %>



<% end %>
<!DOCTYPE html>
<html>
<head>
  <title>BikeSpike</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet"></link>
  <%= csrf_meta_tags %>
</head>
<body class="bikr">
  <header>
          <h2>Bikr</h2>
            </br>
              <ul class="nav navbar-nav">
                <li>
                  <%= link_to "All Bikes", root_path %>
                </li>
                  <% Category.all.each do |category| %>
                <li>
                  <%= link_to category.name, bikes_path(category: category.name) %>
                </li>
                <% end %>
                  <% if user_signed_in? %>
                  <li class="newbike"><%= link_to "Add New Bike", new_bike_path %></li>
                <% else %>
                  <%= link_to "Sign in", new_user_session_path %>
                  <%= link_to "Sign Up", new_user_registration_path  %>
                <% end %>
                </ul>
        </header>

  <div class="container">
      <%= yield %>
   </div> 
   <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</body>

</html>
